/*import { Observable } from "rxjs";
import { HttpResponse } from "@angular/common/http";
import { InjectionToken } from "@angular/core";
export const HTTP_API_PROVIDER_IMPL: InjectionToken<HttpApiProvider> =
  new InjectionToken("HTTP_API_PROVIDER_IMPL");
export interface HttpApiProvider {
  /**
   * Retrieves a list of <T> resources.
   * @param url The url for the get all request
   * @param toModelFn
   */
  /*getAll<U, R>(url: string, toModelFn: (dto: U) => R): Observable<R[]>;
  getNoArg<R>(url: string, options?: any): Observable<R>;
  get<U, R>(
    url: string,
    toModelFn: (dto: U) => R,
    options?: any
  ): Observable<R>;
}
*/